[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && gunicorn allin_django.wsgi --log-file -"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
